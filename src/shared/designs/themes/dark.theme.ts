import { COLOR_DARK } from "../colors/dark.colors";
import { POPPINS_FONT } from "../fonts/poppins.font";
import { METRICS } from "../metrics/metrics";

export const DARK_THEME: ThemeType = {
  COLORS: COLOR_DARK,
  FONTS: {
    PRIMARY: POPPINS_FONT,
  },
  METRICS,
};
